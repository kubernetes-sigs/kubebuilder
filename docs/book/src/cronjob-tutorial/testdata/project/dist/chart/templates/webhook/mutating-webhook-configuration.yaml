apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
    annotations:
        {{- if .Values.certManager.enable }}
        cert-manager.io/inject-ca-from: {{ .Release.Namespace }}/{{ .Values.namePrefix }}-serving-cert
        {{- end }}
    name: {{ .Values.namePrefix }}-mutating-webhook-configuration
webhooks:
    - admissionReviewVersions:
        - v1
      clientConfig:
        service:
            name: {{ .Values.namePrefix }}-webhook-service
            namespace: {{ .Release.Namespace }}
            path: /mutate-batch-tutorial-kubebuilder-io-v1-cronjob
      failurePolicy: Fail
      name: mcronjob-v1.kb.io
      rules:
        - apiGroups:
            - batch.tutorial.kubebuilder.io
          apiVersions:
            - v1
          operations:
            - CREATE
            - UPDATE
          resources:
            - cronjobs
      sideEffects: None
