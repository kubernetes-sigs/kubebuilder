name: Cross-Platform Tests

# Trigger the workflow on pull requests and direct pushes to any branch
on:
  push:
    paths-ignore:
      - '**/*.md'
  pull_request:
    paths-ignore:
      - '**/*.md'

permissions: {}

jobs:
  test:
    permissions:
      contents: read
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
    # Pull requests from the same repository won't trigger this checks as they were already triggered by the push
    if: (github.event_name == 'push' || github.event.pull_request.head.repo.full_name != github.repository)
    steps:
      - name: Clone the code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Setup Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
      # This step is needed as the following one tries to remove
      # kustomize for each test but has no permission to do so
      - name: Remove pre-installed kustomize
        run: sudo rm -f /usr/local/bin/kustomize

      # GitHub's macOS runners have hostnames longer than the 63-byte k8s label
      # limit. kube-apiserver 1.35+ can never create its identity Lease on such
      # hosts and then deadlocks on SIGTERM, so every envtest teardown times out.
      # TODO: remove once https://github.com/kubernetes/kubernetes/issues/140242
      # is fixed in the Kubernetes version used by envtest.
      - name: Shorten hostname (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          sudo scutil --set HostName kb-macos-runner
          hostname

      - name: Unit Tests
        run: make test-unit

      - name: Run Testdata
        run: make test-testdata
